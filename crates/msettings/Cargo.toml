[package]
name = "kui-msettings"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "msettings"
# clean-room drop-in for the legacy libmsettings.so third-party paks link
crate-type = ["cdylib"]

[dependencies]
kui-hal.workspace = true
kui-config.workspace = true
